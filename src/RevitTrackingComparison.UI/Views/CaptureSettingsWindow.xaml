<Window x:Class="RevitTrackingComparison.UI.Views.CaptureSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Capture settings"
        Width="800" Height="580"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <Button DockPanel.Dock="Right" Style="{StaticResource MaterialDesignOutlinedButton}"
                    Content="Reload from model" Command="{Binding ReloadCommand}" />
            <TextBlock TextWrapping="Wrap" VerticalAlignment="Center"
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Opacity="0.72"
                       Text="Pick the parameters to record per category. Categories and parameters are read from the active model." />
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="12">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Text="Categories" />
                        <Button Margin="8,0,0,0" Style="{StaticResource MaterialDesignFlatButton}"
                                Content="All" Command="{Binding SelectAllCategoriesCommand}" />
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Content="None" Command="{Binding ClearAllCategoriesCommand}" />
                    </StackPanel>
                    <ListBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"
                             Style="{StaticResource MaterialDesignListBox}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsIncluded, Mode=TwoWay}"
                                          Style="{StaticResource MaterialDesignCheckBox}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Padding="12">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Text="Parameters" />
                        <Button Margin="8,0,0,0" Style="{StaticResource MaterialDesignFlatButton}"
                                Content="All" Command="{Binding SelectAllParametersCommand}" />
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Content="None" Command="{Binding ClearAllParametersCommand}" />
                    </StackPanel>
                    <ListBox ItemsSource="{Binding SelectedCategory.Parameters}"
                             Style="{StaticResource MaterialDesignListBox}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          Style="{StaticResource MaterialDesignCheckBox}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </materialDesign:Card>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="Save"
                    Command="{Binding SaveCommand}" />
            <TextBlock Margin="16,0,0,0" VerticalAlignment="Center"
                       Foreground="{DynamicResource SecondaryHueMidBrush}"
                       Text="{Binding Status}" />
        </StackPanel>
    </Grid>
</Window>