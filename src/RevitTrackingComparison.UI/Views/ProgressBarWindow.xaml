<Window x:Class="RevitTrackingComparison.UI.Views.ProgressBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding OperationName}"
        Width="420" Height="180"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding OperationName}" FontWeight="Bold" FontSize="14" />
        <TextBlock Grid.Row="1" Margin="0,8,0,0" Text="{Binding OperationStatus}" />
        <ProgressBar Grid.Row="2" Margin="0,8,0,0" Height="18"
                     Minimum="0" Maximum="100" Value="{Binding Percents}" />
        <TextBlock Grid.Row="3" Margin="0,8,0,0"
                   Text="{Binding Elapsed, StringFormat='Elapsed: {0}'}" />

        <Button Grid.Row="4" HorizontalAlignment="Right" Width="100" Height="28"
                Content="Close" Command="{Binding CloseCommand}" />
    </Grid>
</Window>