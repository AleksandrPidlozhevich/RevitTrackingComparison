<Window x:Class="RevitTrackingComparison.UI.Views.SnapshotCompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Compare snapshots"
        Width="580" Height="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center"
                   Text="Base (older):" />
        <ComboBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,12"
                  Style="{StaticResource SnapshotComboBox}"
                  ItemsSource="{Binding Snapshots}" SelectedItem="{Binding From}"
                  DisplayMemberPath="DisplayName" />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center"
                   Text="Compare with (newer):" />
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12"
                  Style="{StaticResource SnapshotComboBox}"
                  ItemsSource="{Binding Snapshots}" SelectedItem="{Binding To}"
                  DisplayMemberPath="DisplayName" />

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,0">
            <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="Compare"
                    Command="{Binding CompareCommand}" />
            <TextBlock Margin="16,0,0,0" VerticalAlignment="Center"
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Opacity="0.72" Text="{Binding Status}" />
        </StackPanel>
    </Grid>
</Window>
